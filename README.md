# ghga_intake_tool
Prototype tool for GHGA data intake process: validates sample metadata (JSON Schema), checks file integrity (existence, size, MD5), and generates machine-readable JSON and human-readable Markdown reports with readiness scores.
